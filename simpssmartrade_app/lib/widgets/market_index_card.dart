import 'package:flutter/material.dart';

class MarketIndexCard extends StatelessWidget {
  const MarketIndexCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Market Index");
  }
}